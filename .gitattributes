# Normalize all text files to LF in the repo
* text=auto eol=lf

# Make sure lockfiles always use LF
package-lock.json text eol=lf
yarn.lock text eol=lf

# Python files (if you have backend here later)
*.py text eol=lf
*.ipynb text eol=lf

# Images and binaries should not be changed by Git
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
